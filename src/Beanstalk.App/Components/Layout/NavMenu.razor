<input type="checkbox" title="Navigation menu" id="navbar-toggler" class="navbar-toggler sr-only"/>
<label for="navbar-toggler" class="hamburger-menu position-fixed top-0 end-0 d-md-none z-50 cursor-pointer d-flex align-items-center justify-content-center bg-white border border-gray-300 rounded shadow-sm hover-bg-indigo-50 hover-border-indigo-300" style="margin: 1rem; width: 44px; height: 44px; cursor: pointer; transition: all 0.2s;">
    <span class="hamburger-icon position-relative">
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
    </span>
</label>

<div class="bg-white border-bottom border-gray-200 px-3 py-3 flex-shrink-0 position-relative">
    <a class="text-decoration-none text-gray-900 fw-semibold hover-text-indigo-600" style="transition: color 0.2s;" href="">🌱 Beanstalk</a>
</div>

<div class="nav-scrollable d-none d-md-flex flex-column flex-md-grow-1 overflow-auto py-3" onclick="document.getElementById('navbar-toggler')?.click()">
    <nav class="d-flex flex-column flex-md-grow-1 px-2" style="gap: 0.25rem;">
        <NavLink class="text-gray-700 rounded px-3 py-2 text-decoration-none d-flex align-items-center hover-bg-indigo-50 hover-text-indigo-700 nav-link-custom" style="min-height: 44px; font-size: 0.875rem; font-weight: 500; transition: all 0.2s;" href="" Match="NavLinkMatch.All">
            Home
        </NavLink>
    </nav>
</div>

<style>
    /* Hamburger icon styling */
    .hamburger-icon {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        width: 20px;
        height: 16px;
        position: relative;
    }
    
    .hamburger-line {
        display: block;
        height: 2.5px;
        width: 100%;
        background-color: #4b5563;
        border-radius: 2px;
        transition: all 0.3s ease;
        transform-origin: center;
    }
    
    .hamburger-menu:hover .hamburger-line {
        background-color: #6366f1;
    }
    
    /* Transform hamburger to X when checked */
    #navbar-toggler:checked ~ .hamburger-menu .hamburger-icon .hamburger-line:nth-child(1) {
        transform: translateY(6.5px) rotate(45deg);
    }
    
    #navbar-toggler:checked ~ .hamburger-menu .hamburger-icon .hamburger-line:nth-child(2) {
        opacity: 0;
        transform: scaleX(0);
    }
    
    #navbar-toggler:checked ~ .hamburger-menu .hamburger-icon .hamburger-line:nth-child(3) {
        transform: translateY(-6.5px) rotate(-45deg);
    }
    
    /* Style the label when checkbox is checked */
    #navbar-toggler:checked ~ .hamburger-menu {
        background-color: #eef2ff;
        border-color: #6366f1;
    }
    
    #navbar-toggler:checked ~ .hamburger-menu .hamburger-line {
        background-color: #6366f1;
    }

    /* Nav link active state */
    .nav-link-custom[data-active="true"] {
        background-color: #eef2ff;
        color: #4f46e5;
        font-weight: 600;
    }
    
    .nav-link-custom:hover {
        font-weight: 600;
    }
    
    @@media (max-width: 767px) {
        /* Hide navigation menu by default on mobile */
        .nav-scrollable {
            display: none !important;
        }
        
        /* Show navigation menu when toggle is checked */
        #navbar-toggler:checked ~ .nav-scrollable {
            display: flex !important;
            flex-direction: column;
            position: fixed;
            top: 4rem;
            left: 0;
            right: 0;
            bottom: 0;
            background: white;
            z-index: 40;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            overflow-y: auto;
            padding: 1rem;
        }
    }
</style>